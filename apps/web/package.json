{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prepare": "pnpm build"
  },
  "devDependencies": {
    "vite": "^3.1.8"
  },
  "dependencies": {
    "@chatemotes/prisma": "workspace:^1.0.0",
    "@zero-dependency/dom": "^0.7.0"
  }
}
